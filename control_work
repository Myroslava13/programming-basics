#include <iostream>
using namespace std;
float suma(int n) {
	float sum = 0, j = 1;
	for (float i = 1;  i<= n; i++) {
		sum = sum + j;
		j = (j * 0.1) + 1;
	}
	return sum;
}
int main() {
	int n= 3 ;

	cout << "Result :" << suma(n);
		return 0;
}
